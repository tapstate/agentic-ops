# Jira 门禁式缺陷修复流程实施计划

> 本计划依据同日设计文档实施。未经用户明确指令，不执行 Git commit 或 push。

## 1. 角色统一

- [x] 将人可见角色统一为“研发工程师”。
- [x] 将协议角色统一为 `development_engineer`。
- [x] 将审查门禁和决策字段统一为 `development_engineer_review`、`development_engineer_decision`。
- [x] 重命名角色文档目录、故事索引、故事编号和内部链接。
- [x] 扫描并确认当前资料和代码中不存在被替代的角色名称。

## 2. Jira 通用写操作

- [x] 为 Jira Description ADF 章节合并编写失败测试。
- [x] 实现保留非目标节点、替换目标章节、追加缺失章节和重复标题拒绝。
- [x] 为任务评论命令编写失败测试并实现。
- [x] 为描述章节更新命令编写失败测试并实现。
- [x] 为逻辑表单字段更新命令编写失败测试并实现。
- [x] 为真实 Jira 确认、负责人、代理所有权和配置错误补充测试。
- [x] 使用 profile `writable` 白名单阻止表单命令修改负责人和所有权字段。
- [x] 让 `inspect-task` 输出 Jira Comment，支持新会话恢复计划与决策事实。
- [x] 根据 operation contract 动态校验写操作允许阶段。
- [x] 增加 Jira 写入前审计，并区分“远端已写入、完成审计失败”状态。
- [x] 输出稳定 JSON 结果和本地审计事件。

## 3. 命令与合同

- [x] 注册 `add-task-comment`。
- [x] 注册 `update-task-description-sections`。
- [x] 注册 `update-task-form`。
- [x] 增加 `-h` 帮助、示例、风险和人工门禁说明。
- [x] 增加三个 operation contract 并校验资产。
- [x] 删除与原子操作边界冲突的 `task run` 编排命令及合同。

## 4. Tapdata 流程资产

- [x] 更新缺陷准入资产，要求代码分析、Jira 补卡评论和重新接管。
- [x] 更新标准流程，增加准入补卡、修复计划确认和实施后回写要求。
- [x] 增加准入分析、补卡确认、修复计划、计划确认和最终证据模板。
- [x] 更新 Tapdata 开发规范和 AI 资产入口。
- [x] 更新 profile 的角色门禁与逻辑字段映射说明。

## 5. 文档与验证

- [x] 更新架构、操作合同、研发工程师指引和用户故事。
- [x] 更新安装资源校验和。
- [x] 运行 Go 单元测试、构建测试、资源测试、安装测试和端到端 fake 流程。
- [x] 运行 `git diff --check` 和旧角色术语扫描。
- [x] 将可复用决策写入个人知识库，不保存凭据或原始敏感日志。
