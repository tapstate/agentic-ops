operation: list_tasks
version: 1
purpose: 列出当前 owner 可处理任务。
task_type: task_listing
allowed_stages:
  - initialized
required_inputs:
  - workspace
side_effects:
  - must_not_write_jira
  - must_not_modify_code
human_gate:
  required: false
