# 复制到工位 config/tm.yml；秘密通过子进程环境注入，不写在模板中。
server:
  address: 127.0.0.1
  port: 3030
spring:
  data:
    mongodb:
      default:
        uri: ${TAPDATA_MONGO_URI}
      obs:
        uri: ${TAPDATA_MONGO_URI}
      log:
        uri: ${TAPDATA_MONGO_URI}
