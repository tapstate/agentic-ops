[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "agentic-ops-internal"
version = "0.1.0"
description = "AgenticOps 仓库内部故事门禁依赖"
readme = "../README.md"
requires-python = ">=3.12,<3.13"
license = { text = "Proprietary" }
authors = [{ name = "TapData" }]
dependencies = [
  "PyYAML>=6.0.2,<7",
]

[tool.uv]
package = false
