# AgenticOps v0.3 AO 真实试运行结果

> **状态：** 试运行主体和第一阶段事实收敛验证已完成；PR、合并与任务完成门禁仍按执行计划推进。

## 1. 试运行范围

- 已发布版本：`INS-v0.3.0-e541e35`
- Jira 项目：`AO`
- Jira 工作项：`AO-2`
- Agentic 运行编号：`AO-2-takeover-20260721103012-a8f3`
- 测试代码工程：`tapstate/agentic-ops`
- 目标分支：`develop`
- 任务分支：`harsen/AO-2/develop`
- 标准流程：`agenticops_improvement_v1`
- 任务分类：`process_improvement`

本次试运行使用隔离安装目录和 AO 项目 AI 工作空间，凭证只注入命令进程，不复制到仓库、计划或证据文件。试运行不修改 v0.3 已发布二进制；已证实代码问题进入 v0.4 设计与实施计划。

## 2. 发布和平台事实

- v0.3 已通过 PR #2 使用 Merge commit 合入 `main`，合并提交为 `fa7402a`。
- 远端 annotated Tag `v0.3` 已存在；源码发布完成审计已生成。
- 仓库为私有仓库，GitHub 默认分支为 `main`，允许 Merge commit，未启用 Auto-merge。
- 当前套餐访问私有仓库 Ruleset API 返回 403，不能形成服务器端 PR-only 硬门禁。
- 当前 clone 已启用项目 Hooks；发布脚本、固定发布分支、人工 Merge commit、合并事实校验、二次完整验证和审计共同构成显式软门禁。
- 项目接受软门禁不能阻止其它 clone、`--no-verify` 或网页操作直接修改 `main` 的剩余风险，不再保留套餐升级待办。
- 可选 GitHub Release 制品页能力未实现，但它不等同于源码发布，也不阻塞当前正式研发流程。

## 3. Jira 门禁执行记录

1. 创建 AO-2 后回读确认：工作类型为“Agentic 缺陷”，负责人是当前研发工程师，执行模式为“研发模式”，目标分支为 `develop`，初始状态为“待接管”。
2. v0.3 `inspect-task` 正确解析任务分类、标准流程、目标仓库和转换 `2`。
3. 研发工程师明确确认接管后执行 `takeover-task --confirm-real-jira-write`。
4. CLI 写入 Agentic 字段并返回成功，但 Jira 状态仍为“待接管”；按原接管授权补执行转换 `2`，回读状态为“执行中”。
5. 修正计划 v1 经研发工程师确认写入，Jira `plan` 评论 ID 为 `46513`。
6. 研发工程师单独确认计划内容后，Jira `decision` 评论 ID 为 `46514`；任务保持“执行中”，Agentic 绑定和运行编号未改变。

评论恢复写入都保留 `AO-2`、工作空间和运行编号，并通过 Jira 回读确认内容；没有把恢复动作伪装成 v0.3 CLI 成功，也没有重写原始本地事件。

## 4. 已证实问题

### 4.1 自定义安装目录污染真实 shell profile

**证据：** 使用自定义 `AGENTIC_OPS_HOME` 隔离安装时，安装脚本仍把临时 `bin` 路径写入真实用户 shell profile。

**根因方向：** profile 路径按真实用户 Home 解析，而 PATH 内容按自定义安装目录生成；现有测试只覆盖临时 Home 与默认安装目录组合。

**恢复：** 已精确删除本次新增的临时 PATH 行，并确认不存在残留。

### 4.2 运行时使用固定时间

**证据：** 接管输出、运行编号、Jira 接管时间、心跳和本地事件都使用 `2026-07-21T10:30:12Z`，与真实执行日期不一致；源码中的 `fixedNow()` 直接返回该固定值。

**影响：** 运行编号可能重复，审计时间不可信，心跳无法反映代理存活状态，基于时间的反馈和恢复判断可能错误。

### 4.3 接管字段与状态转换未原子执行

**证据：** `takeover-task` 更新所有权字段后返回 `ok:true`，但实现没有调用 Jira transition，AO-2 仍处于“待接管”。AO 流程设计要求转换 `2` 与 Agentic 字段在同一 Jira 转换请求中原子完成。

**影响：** 可能出现字段已绑定但状态仍允许接管的部分成功，并发代理无法依靠 Jira 转换获得唯一锁；CLI 成功结论与 Jira 事实冲突。

**恢复：** 回读确认部分写入后没有重复接管，只补执行已授权的转换 `2`，再回读为“执行中”。该恢复无法恢复原子性，因此必须保留为缺陷证据。

### 4.4 首条不完整门禁事件破坏运行上下文

**证据：** 同一次真实接管先写入一条缺少 `task_class` 和 `process_id` 的成功门禁事件，再写完整接管事件。`RunContextReader` 选择第一条成功 `takeover_task` 作为锚点，导致后续带正确 `--run-id` 的 `add-task-comment` 返回 `local_state_mismatch`。

**影响：** 真实接管成功后，计划、决策、证据和 PR 证据等需要运行上下文的后续操作会被错误阻断；若省略运行编号绕过，会破坏任务审计关联。

**恢复：** 不省略运行编号、不篡改事件文件、不重复失败命令；在所有权、状态和运行编号回读一致后，按 v0.3 相同 ADF 格式执行单次评论恢复写入，并记录本地审计缺口。

### 4.5 默认证据不符合中文与验收要求

**证据：** v0.3 `write-evidence` 使用的 `development-completed.md` 标题和字段说明为英文，只包含工作空间、运行编号、任务分类、流程、阶段、仓库和固定 `status`，没有变更内容、验证命令、结果、风险或恢复说明。

**影响：** 直接写入会违反 AO 项目 Jira 人可见内容使用中文的规则，也不能单独证明任务验收结果；运行上下文缺陷还会在生成本地文件前阻断命令。

**恢复：** 在隔离工作空间生成中文、脱敏的证据主体，包含变更、验证、五个问题和审计缺口；经独立人工确认后再单次写入 Jira，不声称 v0.3 已返回 `audit_submitted=true`。

## 5. 权威事实收敛

本次修改完成以下收敛：

- 新增长期 GitHub Free 私有仓库软门禁决策，并修正“受保护 PR”的无条件表述。
- 关闭套餐升级待办，显式记录服务器端直推剩余风险。
- 把 Task 6 更新、回滚和 `exact_pair` 治理标记为已完成。
- 把 v0.3 源码发布标记为已完成。
- 把 Task 7 限定为可选 GitHub Release 制品页能力，不作为源码发布或正式使用阻塞项。
- 当前直接推进项改为真实 AO 试运行及基于证据的 v0.4 改进。

## 6. 验证结果

| 验证 | 结果 | 证据摘要 |
| --- | --- | --- |
| 当前事实关键词检查 | 通过 | 当前权威文档不存在套餐升级待办、无条件“受保护 PR”或把可选 GitHub Release 当作阻塞项的旧表述 |
| `git diff --check` | 通过 | 无空白或补丁格式错误 |
| `bash scripts/test-release-workflow.sh` | 通过 | `operation=test_release_workflow`，`cases=53` |
| `bash scripts/test-resources.sh` | 通过 | `operation=test_resources`，`ok=true` |
| `go test ./...` | 通过 | 沙箱外使用同一命令执行，所有 Go package 通过 |

Go 测试首次在受限沙箱内因 `httptest` 无法绑定本地端口、Go 缓存不可写而失败；这不是测试断言失败。按原命令在允许本地测试端口和 Go 缓存的受控环境重跑后通过。

## 7. 结论和下一步

v0.3 已证明 AO profile、真实 Jira 读取、所有权字段写入和人工门禁可以连接真实流程，但接管原子性、真实时间、运行上下文和隔离安装副作用尚不满足可靠日常使用要求。不能仅依据 CLI `ok:true` 判断外部写入完成，必须回读 Jira 事实。

下一步根据以上五个问题形成 v0.4 改进设计和实施计划，至少覆盖：可注入真实时钟、AO 原子接管、运行上下文锚点选择与事件语义、隔离安装 profile 边界、中文验收证据、失败恢复和端到端测试。实现工作在后续 v0.4 任务中按测试驱动方式推进，不在本次文档事实收敛中临时修改运行代码。
